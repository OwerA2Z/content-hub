import { useEffect, useState } from "react";
import { Alert, Button, Card, Empty, Input, Space, Typography } from "antd";
import { planningApi } from "../../lib/api/planning";
import type { Brief, Series, Strategy } from "../../lib/api/types";

export function PlanningPage() {
  const [strategies, setStrategies] = useState<Strategy[]>([]); const [series, setSeries] = useState<Series[]>([]); const [briefs, setBriefs] = useState<Brief[]>([]); const [strategyId, setStrategyId] = useState(""); const [seriesId, setSeriesId] = useState(""); const [notice, setNotice] = useState(""); const [name, setName] = useState(""); const [goal, setGoal] = useState(""); const [seriesName, setSeriesName] = useState(""); const [briefTitle, setBriefTitle] = useState("");
  useEffect(() => { planningApi.listStrategies().then((result) => { setStrategies(result.data); if (result.data[0]) setStrategyId(result.data[0].id); }).catch(() => setNotice("加载内容战略失败")); }, []);
  useEffect(() => { if (!strategyId) return; planningApi.listSeries(strategyId).then((result) => { setSeries(result.data); setSeriesId(result.data[0]?.id ?? ""); }).catch(() => setNotice("加载内容系列失败")); }, [strategyId]);
  useEffect(() => { if (!seriesId) return setBriefs([]); planningApi.listBriefs(seriesId).then((result) => setBriefs(result.data)).catch(() => setNotice("加载文章任务失败")); }, [seriesId]);
  return <div className="app-page"><section className="app-hero app-hero--compact"><div><Typography.Text className="app-eyebrow">CONTENT PLANNING</Typography.Text><Typography.Title level={2}>让每篇文章都有下一步。</Typography.Title><Typography.Paragraph>通过战略、系列和文章任务，帮助 AI 连续产出内容。</Typography.Paragraph></div></section><div className="app-planning-grid"><Card className="app-panel-card" title="内容战略"><div className="app-card-stack"><Input placeholder="战略名称" value={name} onChange={(event) => setName(event.target.value)} /><Input.TextArea rows={3} placeholder="战略目标" value={goal} onChange={(event) => setGoal(event.target.value)} /><Button type="primary" onClick={() => planningApi.createStrategy({ name, goal, contentPillars: [], avoidTopics: [] }).then((result) => { setStrategies((items) => [result.data, ...items]); setStrategyId(result.data.id); setName(""); setGoal(""); setNotice("战略已创建"); }).catch(() => setNotice("创建战略失败"))}>创建战略</Button></div><div className="app-selection-list">{strategies.map((item) => <Button type="text" className={`app-selection-item ${strategyId === item.id ? "app-selection-item--active" : ""}`} key={item.id} onClick={() => setStrategyId(item.id)}><strong>{item.name}</strong><Typography.Text type="secondary">{item.goal}</Typography.Text></Button>)}</div></Card><Card className="app-panel-card" title="内容系列">{strategyId ? <><div className="app-card-stack"><Input placeholder="系列名称" value={seriesName} onChange={(event) => setSeriesName(event.target.value)} /><Button type="primary" onClick={() => planningApi.createSeries(strategyId, { sequence: series.length + 1, name: seriesName, targetCount: 1, orderMode: "sequential" }).then((result) => { setSeries((items) => [...items, result.data]); setSeriesId(result.data.id); setSeriesName(""); setNotice("系列已创建"); }).catch(() => setNotice("创建系列失败"))}>创建系列</Button></div><div className="app-selection-list">{series.map((item) => <Button type="text" className={`app-selection-item ${seriesId === item.id ? "app-selection-item--active" : ""}`} key={item.id} onClick={() => setSeriesId(item.id)}><strong>#{item.sequence} {item.name}</strong><Typography.Text type="secondary">目标 {item.targetCount} 篇 · {item.status}</Typography.Text></Button>)}</div></> : <Empty description="请先选择战略" />}</Card></div><Card className="app-panel-card" title="文章任务">{seriesId ? <><Space.Compact block><Input placeholder="标题方向" value={briefTitle} onChange={(event) => setBriefTitle(event.target.value)} /><Button type="primary" onClick={() => planningApi.createBrief(seriesId, { sequence: briefs.length + 1, titleDirection: briefTitle, mustCover: [], mustAvoid: [] }).then((result) => { setBriefs((items) => [...items, result.data]); setBriefTitle(""); setNotice("文章任务已创建"); }).catch(() => setNotice("创建任务失败"))}>创建任务</Button></Space.Compact><div className="app-brief-list">{briefs.map((item) => <div className="app-brief" key={item.id}><strong>#{item.sequence} {item.titleDirection}</strong><Typography.Text type="secondary">{item.status}</Typography.Text></div>)}</div></> : <Empty description="请先选择系列" />}{notice && <Alert style={{ marginTop: 16 }} type="info" showIcon message={notice} />}</Card></div>;
}
